# Release 0.5.11

## Highlights

- Mobile image export now saves files directly into vault folder:
  - `<Collection folder>/exports`
- Added notice showing vault path after export on mobile.
- Export workflow remains unchanged on desktop:
  - files are downloaded via browser download flow.

## Internal

- Added platform-aware export target selection.
- Added helper methods for image export path and vault binary writes.

